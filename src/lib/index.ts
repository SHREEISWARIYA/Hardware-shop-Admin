import { API_URL, AUTH_COOKIE } from "./config/config";
import { handleLogout, handleLogin, handleGetOTP } from "./utils/auth";
export { API_URL, AUTH_COOKIE, handleLogin, handleLogout, handleGetOTP }